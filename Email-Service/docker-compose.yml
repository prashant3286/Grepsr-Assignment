version: '3.8'

services:
  app:
    build: .
    depends_on:
      - mongo
      - rabbitmq
    ports:
      - 8000:8000
    environment:
      - DATABASE=mongodb://mongo:27017/emaildb
      - RABBIT_URL=amqp://rabbitmq:5672
    networks:
      - rabbitmq_nodejs

  mongo:
    image: mongo
    restart: always
    ports:
      - 27017:27017
    logging:
      driver: none
    volumes:
      - ./mongo/data:/data/db

  rabbitmq:
    image: rabbitmq
    restart: always
    ports:
      - 5672:5672
      - 15672:15672
    logging:
      driver: none
    networks:
      - rabbitmq_nodejs

networks:
  rabbitmq_nodejs:
    driver: bridge
